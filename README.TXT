

v0.4.0 — current
├── Layered architecture
├── `core.py`, `terminal.py`, `validations.py` and `view_terminal.py`
├── Reusable core without terminal input/output
├── Expanded motorcycle and maintenance data
├── Plate validation and normalization
├── Monetary values stored in cents
├── Advanced combined filters
├── Improved relational integrity handling
├── Centralized transaction control
├── Standardized terminal visualization

v0.3.0
├── Relational database structure
├── `clientes`, `motos` and `manutencoes` tables
├── Foreign key relationships
├── Full relational CRUD
├── SQL `JOIN` and `LEFT JOIN`
├── Maintenance history
├── Search across related tables
├── Client, motorcycle and maintenance editing
├── Relational deletion with integrity protection
├── `tabelas.py` separated for data visualization
├── Improved table formatting
├── Standardized date storage and display
└── Improved menu and exit flow

v0.2.0
├── `validacoes.py` separated
├── improved module responsibilities
├── `main()` + `if __name__ == "__main__"`
├── more organized menu
├── unified search
├── partial search using `LIKE`
├── exact ID search
├── date search and validation
└── improved input handling

v0.1.0
├── Functional CRUD
├── SQLite
├── main.py + database.py
├── Validations
├── Exit system
└── Simple column-based search
├── Maintenance summary by period
└── Architecture prepared for future GUI
